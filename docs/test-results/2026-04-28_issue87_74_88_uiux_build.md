# #87 / #74 / #88 UI/UX 改善ビルド確認

## 実施日
- 2026-04-28

## 対象
- `#87` タイムラインをセグメント作業面として強化
- `#74` パスコピーと定量進捗表示
- `#88` 設定画面の重複整理と出力パス操作

## 確認内容
- 同一フレーム内の OCR detection を bounding box 上端座標順でタイムラインへ表示する実装を確認。
- タイムラインの複数行表示、列幅ドラッグ変更、認識精度ゲージを実装。
- メイン画面の結果ペインを削除し、処理状況と定量進捗を右ペインへ集約。
- 出力、ログ、作業ディレクトリのコピー操作を追加。
- 設定画面の重複セクションを削除し、出力関連パスの Copy / Open 操作を追加。

## 実行コマンド
```powershell
dotnet build src\MovieTelopTranscriber.sln -p:Platform=x64
```

## 結果
- ビルド成功。
- 警告 0 件。
- エラー 0 件。

## 手動確認が必要な項目
- 実動画で複数 OCR detection の上下順が期待通りか確認する。
- タイムライン列幅ドラッグの操作感を確認する。
- Copy 操作で期待するパスがクリップボードへ入ることを確認する。
- Open 操作で該当フォルダまたはファイル位置が Explorer で開くことを確認する。
