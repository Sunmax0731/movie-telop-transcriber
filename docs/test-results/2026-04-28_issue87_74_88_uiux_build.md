# #87 / #74 / #88 UI/UX 改善ビルド確認

## 実施日
- 2026-04-28

## 対象
- `#87` タイムラインをセグメント作業面として強化
- `#74` パスコピーと定量進捗表示
- `#88` 設定画面の重複整理と出力先フォルダ設定

## 確認内容
- 同一フレーム内の OCR detection を bounding box 上端座標順でタイムラインへ表示する実装を確認。
- タイムラインの複数行表示、列境界線付きの列幅ドラッグ変更、認識精度ゲージを実装。
- メイン画面の結果ペインを削除し、右ペインにタイムライン、中央ペインに処理状況と定量進捗を配置。
- 中央ペインと右ペインの境界ドラッグで右ペイン幅を変更する実装を確認。
- 出力、ログ、作業ディレクトリのコピー操作を追加。
- 設定画面の重複セクションと右ペインを削除し、左ペインに出力先フォルダ設定を追加。

## 実行コマンド
```powershell
dotnet build src\MovieTelopTranscriber.sln -p:Platform=x64
```

## 結果
- ビルド成功。
- 警告 0 件。
- エラー 0 件。

## 手動確認が必要な項目
- 実動画で複数 OCR detection の上下順が期待通りか確認する。
- タイムライン列幅ドラッグと右ペイン幅ドラッグの操作感を確認する。
- 認識精度のパーセント表記と横方向ゲージが同時に表示されることを確認する。
- Copy 操作で期待するパスがクリップボードへ入ることを確認する。
- 設定画面で出力先フォルダを編集または参照選択できることを確認する。
