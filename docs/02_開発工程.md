# 開発工程

## 1. 方針
本システムは、一般的なシステム開発工程に沿って段階的に進める。各工程で成果物と確認観点を定義し、次工程へ進む条件を明確にする。

## 2. 工程一覧
1. 要件定義
2. 仕様検討
3. 設計
4. 実装
5. テスト
6. リリース

## 3. 各工程の内容
### 3.1 要件定義
目的:
- システムの目的、対象範囲、利用者、機能要件、非機能要件を定める。

主な成果物:
- `docs/01_要件定義.md`

完了条件:
- GUI 前提、オフライン前提、主要機能、制約条件が文書化されていること。
- 次工程である仕様検討の親 Issue と初期タスク Issue が起票されていること。
- 工程完了レビューとして `AGENTS.md`、`SKILL.md`、`README.md`、関連 `docs/` の整合確認が終わっていること。

### 3.2 仕様検討
目的:
- 要件を満たすための具体仕様を定める。

主な検討項目:
- 動画入力仕様
- フレーム抽出仕様
- OCR 処理仕様
- テロップ属性記録仕様
- 結果統合仕様
- 出力形式仕様
- エラー時の扱い

主な成果物:
- 仕様書
- データスキーマ案

完了条件:
- 実装者が画面、処理、データ形式を迷わず作業できる粒度になっていること。
- 次工程である設計の親 Issue と初期タスク Issue が起票されていること。
- 工程完了レビューとして `AGENTS.md`、`SKILL.md`、`README.md`、関連 `docs/` の整合確認が終わっていること。

### 3.3 設計
目的:
- 仕様を実装可能な構成に落とし込む。

主な検討項目:
- 画面設計
- モジュール設計
- データ構造設計
- ファイル配置設計
- ログ設計

主な成果物:
- 基本設計書
- 詳細設計書
- 画面遷移または画面構成案

完了条件:
- 実装タスクへ分割可能であり、依存関係が整理されていること。
- 次工程である実装の親 Issue と初期タスク Issue が起票されていること。
- 工程完了レビューとして `AGENTS.md`、`SKILL.md`、`README.md`、関連 `docs/` の整合確認が終わっていること。

### 3.4 実装
目的:
- 設計に基づきアプリケーションを構築する。

主な作業:
- GUI 実装
- 動画読み込み実装
- フレーム抽出実装
- OCR 実装
- 属性解析実装
- 統合処理実装
- 出力実装

完了条件:
- 仕様上の対象機能が動作し、既知制約が記録されていること。
- 次工程であるテストの親 Issue と初期タスク Issue が起票されていること。
- 工程完了レビューとして `AGENTS.md`、`SKILL.md`、`README.md`、関連 `docs/` の整合確認が終わっていること。

### 3.5 テスト
目的:
- 仕様と実装の整合を確認し、品質を担保する。

主なテスト:
- 単体テスト
- 結合テスト
- GUI 操作テスト
- オフライン動作確認
- サンプル動画での精度確認

主な成果物:
- テスト計画書
- テスト結果
- 既知不具合一覧

完了条件:
- 主要シナリオが通過し、重大不具合が解消されていること。
- 次工程であるリリースの親 Issue と初期タスク Issue が起票されていること。
- 工程完了レビューとして `AGENTS.md`、`SKILL.md`、`README.md`、関連 `docs/` の整合確認が終わっていること。

### 3.6 リリース
目的:
- 利用者が使える形で配布可能にする。

主な作業:
- 配布物作成
- 使い方文書整備
- バージョン整理
- リリースノート作成

完了条件:
- オフライン環境で導入と利用が可能な配布形態が準備されていること。
- 工程完了レビューとして `AGENTS.md`、`SKILL.md`、`README.md`、関連 `docs/` の整合確認が終わっていること。

## 4. 直近の進め方
- 現在工程: 初期リリース `v0.1.0` 公開済み。リリース後改善へ移行する。
- 前工程: テスト
- 実装工程の親 Issue `#19` と個別 Issue `#31`、`#33`、`#39`、`#40`、`#41` は完了済み。
- テスト工程の親 Issue `#21` は完了済み。
- `#42` で `docs/06_テスト計画書.md` を作成済み。
- `#43` で `test-data/basic_telop/` の検証用サンプル動画と正解データを整備済み。
- `#44` で GUI 主要操作シナリオ結果を記録し、発見された UI 不整合を修正済み。
- `#45` で OCR と属性解析の精度検証結果を `docs/test-results/2026-04-28_ocr属性解析精度検証.md` に記録済み。
- `#46` でオフライン導入と実行検証結果を `docs/test-results/2026-04-28_オフライン導入実行検証.md` に記録済み。
- `#47` で既知不具合と制約一覧を `docs/08_既知不具合と制約一覧.md` に整理済み。
- テスト工程の個別 Issue `#42` から `#47` は完了済み。
- `#21` のテスト工程完了レビューは `docs/test-results/2026-04-28_テスト工程完了レビュー.md` に記録済み。
- リリース工程の親 Issue は `#18`。
- リリース工程の着手済み個別 Issue は `#48`、`#49`、`#50`、`#51`、`#72`、`#74`、`#75`、`#84`、`#85`、`#86`、`#87`、`#88`、`#89`、`#90`、`#93`、`#95`、`#100`、`#102`。
- `#87`、`#74`、`#88` では、タイムライン作業面、処理状況、設定画面の重複整理をリリース前改善として完了済み。
- `#93` では、タイムライン編集、認識精度ゲージの 50% 起点表示、プレビュー再生、左ペインと設定画面の責務整理、出力先フォルダの事前検証を進める。
- `#95` では、Desktop 日本語パスでの PaddleOCR stdio エンコード修正、プレビュー位置操作、処理時間表示、設定スライダー化、初期設定復帰、処理状況レイアウト整理、タイムライン列表示切替、選択テキストコピー、記号補完検出、表示属性列整理、設定画面多言語化を進める。
- `#86` では、タイムライン上の OCR / セグメント結果に対する編集履歴出力を進める。結合 / 分割はプレビュー同期仕様を再検討するまで UI から非表示とする。
- `#89` では、セグメント結果から SRT / VTT / ASS 字幕ファイルを出力する。
- `#100` では、プレビューとタイムラインの初期幅を揃え、プレビューと処理状況の境界ドラッグで処理状況ペインの高さを変更できるようにする。
- `#102` では、OCR エンジン未指定時の既定を PaddleOCR にし、`json-sidecar` 0 件成功への誤起動を防止する。
- `#90` では、QCDS 評価用の代表動画条件、計測指標、レポートテンプレート、`basic_telop` の初期計測レポートを整備する。
- `#75` では、初期リリースで保証するテロップ属性範囲と、既知制約として残す属性を整理済み。
- リリース前 UI/UX・QCDS 改善トラック `#84` は、`#75` 完了後に完了判断する。
- `#48` で `docs/11_配布構成と同梱物.md` を作成し、初期リリースの配布構成、同梱物、非同梱物、配布サイズ、制約を整理済み。
- `#49` で `docs/12_導入手順書.md` を作成し、Release build 出力、.NET 10 Desktop Runtime、PaddleOCR Python 環境、モデル事前配置、初回確認、導入失敗時の確認項目を整理済み。
- `#50` で `docs/13_リリースノート.md` を作成し、`v0.1.0` の提供範囲、導入前提、既知制約、今後の予定を整理済み。
- `#51` で `tools/release/New-ReleasePackage.ps1`、配布 zip、checksum、GitHub Release `v0.1.0` を整備済み。
- GitHub Release: `https://github.com/Sunmax0731/movie-telop-transcriber/releases/tag/v0.1.0`
- `#114` で README 再構成、導入インストーラ、PaddleOCR モデル取得 warmup を整備済み。
- `#116` でインストーラ同梱版 `v0.1.1` の配布を進める。
- リリース工程の親 Issue `#18` は、工程完了レビュー後に close する。
- 次に着手する主な open Issue は `#98` タイムライン結合・分割 UI とプレビュー同期の再設計。

## 5. Issue 駆動運用
- GitHub 上に工程ごとのマイルストーンを作成する。
- 各工程には親 Issue を 1 件作成し、工程全体の完了条件と成果物を管理する。
- 現在工程の具体タスクは Issue として起票する。
- Issue は、文書更新、比較検証、方針決定、実装準備などの完了単位で分割する。
- 原則として Issue ごとに専用ブランチを切って作業する。
- ブランチ名は `issue-<番号>-<要約>` 形式を基本とする。
- 工程完了時は、対応 Issue の完了と成果物の整合を確認してから次工程へ進む。
- 工程完了時は、次工程の親 Issue と初期タスク Issue を先に起票する。

## 6. 工程境界レビュー
- 各工程の完了時に、少なくとも以下を見直す。
  - `AGENTS.md`
  - `SKILL.md`
  - `README.md`
  - 当該工程に関連する `docs/`
- 見直しでは、現在の方針、GitHub 運用、実装状態、次工程の開始条件に齟齬がないことを確認する。
